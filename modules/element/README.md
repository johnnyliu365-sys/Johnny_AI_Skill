# Ticket Element 索引規範

`modules/element/` 是工單與實際程式碼之間的可審閱索引，不得複製正式原始碼。通用程式碼將在 SPEC 核准後置於 `library/`，並由每張已核准工單建立對應 element。

- 每個 element 必須列出領域型別、應用流程、實際程式碼、公開契約、TDD 與驗證證據。
- 目前尚未有已核准的實作工單，因此沒有 element 子項。
