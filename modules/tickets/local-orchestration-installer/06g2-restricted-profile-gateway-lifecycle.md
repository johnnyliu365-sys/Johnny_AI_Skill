# 06G2 — Restricted-profile and Gateway Owned Lifecycle

| Field | Value |
| --- | --- |
| SPEC / AC | Local installer SPEC revision 03 / AC-09 and AC-10 |
| Change | `CHG-20260814-018` |
| State | `PLANNED / DEPENDENCY_WAIT / NOT_DISPATCHED` |
| Dependency | 06G1 independently approved and integrated |
| Delivery profile | `HIGH_ASSURANCE`; one implementer; no helper |

## One outcome

In the disposable lifecycle only, install/read back/receipt-bind/remove and
prove absence for the exact restricted implementation profile and local Johnny
gateway registration/state. Preserve same-name foreign profiles, unrelated
gateway/global settings and two external target sentinels byte-for-byte. Missing,
tampered, partial, foreign or replayed ownership blocks without broad deletion.

The ticket must separately freeze public lifecycle models, effect ports,
constructor preflight, failure matrix, compensation order and reversals. It may
not start a model task, edit live Codex home, add MCP/network service, package,
install for real, push, release or deploy. `XSS_NOT_APPLICABLE`.
