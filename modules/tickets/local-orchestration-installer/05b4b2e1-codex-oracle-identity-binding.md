# 05B4B2E1 — Codex Oracle Identity Binding

| Field | Value |
| --- | --- |
| State | `PLANNED / DEPENDENCY_WAIT / NOT_DISPATCHED` |
| Dependency | 05B4B2E0 approved and integrated |
| Allocation | None |

## Reserved responsibility

Add one pure test-staging binding that recursively validates an exact
`CodexRegistrationPortRequest` and derives one exact `OracleIdentity`, including
canonical logical marketplace/plugin paths. Fixed oracle-only `source` and
`installPolicy` labels must be named constants and may never become receipt
authority. Alternate, malformed or constructed request values fail closed.
No oracle command or other effect is invoked. Exact closure and writable paths
must be frozen after E0 integration. No numeric line limit applies.
