# 06C — Reviewer-role Composition Gate

| Field | Value |
| --- | --- |
| SPEC / AC | Local installer SPEC revision 02 / AC-02, AC-09 and AC-10 |
| State | `PLANNED / DEPENDENCY_WAIT` |
| Closure | `CLOSURE-LOCAL-INSTALL-T06C-01` / C1-C4 |
| Implementation language | Python 3.11 with strict Pydantic models and `mypy --strict` |
| Dependency | Autonomous Ticket 04 and local 06B independently approved/integrated |
| Responsibility | Existing implementation task/worktree after a unique ticket receipt; control `main` reviews |

## One outcome

Compose the receipt-owned Codex profiles with the typed reviewer-only Router
authority gate. One exact reviewer action reaches a recorded host fake once;
every implementation direct/indirect action blocks before the host. Project,
ticket, handoff, receipt, target and correlation replay remain exact.

## Acceptance closure — `CLOSURE-LOCAL-INSTALL-T06C-01`

- `C1`: installer support projects only when 06A capability, 06B profile
  receipt and Ticket-04 authority contracts all match one installation/version.
- `C2`: exact reviewer create/dispatch/follow-up/steer/wait/interrupt/close
  reaches one fake effect each under its own consumable binding.
- `C3`: implementation profile direct and indirect paths, generic control-plane,
  forged reviewer, wrong target/ticket/handoff/correlation and replay return
  `ROLE_FORBIDDEN` or the exact receipt error with zero effects.
- `C4`: install/remove composition proves profile absence, foreign preservation
  and target-project non-interference. It grants no real model turn or deploy.

Reverse the role check, profile capability readback and receipt consumption.
Return one implementation commit and one docs-only handoff. Approval unblocks
the Windows package ticket's reviewer-role dependency only.
