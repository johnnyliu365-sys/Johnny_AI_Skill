# V2-S3 — 找回「這一段還沒做完」的訊號

| Field | Value |
| --- | --- |
| State | 排隊中，等 V2-S2 完成 |
| Owner | UI implementation owner |
| Files | `library/local_orchestration/ticket_status_template.py` 與它的測試 |
| Depends on | V2-S2 |

## 一個結果

未完成的階段方塊重新有「看這裡」的份量，而且不再與任何狀態色碰撞。

## 背景

核准的樣張裡，未完成階段是琥珀色外框加粗體——一個真的會抓住視線的訊號。為了解決綠色衝突，階段方塊被整組改成無彩色，你當時就講明這是有代價的：外框加粗比琥珀安靜。

`DONE` 改成灰色之後，無彩色的階段方塊處境更差了：它現在和一個狀態徽章共用無彩色。

## 邊界

不能重新引入會與五種狀態撞色的顏色。也不要讓未完成方塊比整列的狀態更搶眼——階段是列的細節，不是列的結論。

你上一輪提過的備案（一個離草綠 45° 以上的階段綠）仍然可用，但那會讓階段方塊重新進入色相競爭；如果你有不靠色相的做法，優先採用。

## 驗收

| Ref | 要求 | 證據 |
| --- | --- | --- |
| S3-1 | 未完成方塊與完成方塊的差異可量測且達到你設定的門檻 | 測試 |
| S3-2 | 不與任何狀態色碰撞 | 沿用色相／無彩測試 |
| S3-3 | 階段方塊不比列的狀態搶眼 | 響度斷言 |
| S3-4 | 反向突變 | 把兩種方塊做成相同外觀，S3-1 要轉紅 |

```johnny-status
id = V2-S3
title = 找回未完成階段的訊號
state = IN_PROGRESS
stage = D | 設計 | OPEN
stage = T | 測試 | OPEN
```
