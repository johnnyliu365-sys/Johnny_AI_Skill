# Plugin Distribution Context

## Wayfinder 決策

```text
Decision: GO
Target: Johnny AI Skill Codex plugin bundle
Problem: skill-only Git distribution cannot execute or verify the Router, reversible runtime, event supervision and clean package boundary promised by the detachable control plane.
POC: build one deterministic versioned Codex ZIP containing the plugin-owned governance, skills, reusable library, typed Router runtime and reversible bootstrap while preserving target-project isolation.
Success: the exact ZIP installs into a clean user boundary, runs the scripted control matrix, attempts one real SourceProjectA role smoke, removes every owned effect and leaves the original target repository unchanged.
Out of scope: Setup.exe, MCP/App/hook, heartbeat, automation, polling, system-wide install, silent tool installation, target runtime dependency, Provider, Secret, push, release and deployment.
Authority: the project owner approved the complete Codex bundle, version 0.4.0, deterministic ZIP/SHA-256, isolated venv, event runner, fail-closed host wake gate and SourceProjectA verification decisions on 2026-08-17.
```

## Revision 02 Grill 收斂

- The formal artifact is `johnny-ai-skill-0.4.0.zip` plus an externally recorded SHA-256. A clean source commit must reproduce identical bytes.
- The payload is an allowlist: Codex manifest, `skills/`, `library/`, plugin governance, existing README, bootstrap/CLI, dependency lock and payload manifest. Development, target-owned and runtime evidence trees do not enter the ZIP.
- Ticket admission binds versioned Profile `plugin-distribution-poc-r02` from the approved SPEC. Its only builder is `library/workflow_router/profile.py::build_plugin_distribution_profile`; lock, manifest, bundle and CLI contracts use the exact `library/local_orchestration` locations declared by that SPEC.
- Router decisions are short-lived Python executions. No MCP server is introduced.
- One temporary runner exists per controlled project while tickets are active. It multiplexes one exact receipt-bound Git subscription per ticket through Windows native notifications, never polling.
- The runner can outlive a terminal but cannot register an OS startup task, service, schedule or watchdog. After reboot, the next explicit plugin invocation may recover exact active metadata and Git ref ancestry.
- Git notification proves only a completion candidate. Automatic Senior wake is supported only when a production host adapter proves an exact receipt-bound event/callback capability. The current host does not expose that capability, so manual forwarding is a bootstrap fallback and Router binding remains blocked.
- Existing Codex roles are legal only after explicit user designation and exact role/model/task/thread/host/worktree/branch/baseline/receipt binding. Plugin creation is not authority.
- Python uses a compatible user-installed interpreter to create a Johnny-owned venv. Core dependencies are hash-locked; optional integrations are lazy and cannot break core import.
- Install and uninstall are explicit reversible user actions. `johnny-router uninstall` is the only removal route allowed to claim complete Johnny runtime cleanup.
- The original SourceProjectA repository is read-only. Full scripted verification and one isolated real-role smoke run only in a disposable Johnny-owned copy and are deleted afterward.

## Ownership and portability

- Plugin governance, runtime, receipts, queue, telemetry and dependency environment remain Johnny-owned and user-scoped.
- Target Context, PRD/CHG, SPEC, tickets, review evidence, source and tests remain target-owned and target-versioned.
- Removing Johnny never removes or rewrites target artifacts, Git history or the target team's ability to continue with its own process.
- Publication, tag, push, GitHub release and deployment require separate owner authority after the exact candidate digest passes.
